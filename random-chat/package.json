{
  "name": "random-chat",
  "version": "1.0.0",
  "description": "ShincChat — verified random chat (text + video). Multi-process capable: stateless JWT sessions, Redis-backed matching/presence, object-storage media.",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "start:cluster": "node server/boot.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/s3-request-presigner": "^3.700.0",
    "@socket.io/redis-adapter": "^8.3.0",
    "@upstash/redis": "^1.34.3",
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "geoip-lite": "^1.4.10",
    "ioredis": "^5.4.2",
    "pg": "^8.13.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
